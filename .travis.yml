language: scala

matrix:
  include:
    - os: linux
      sudo: false
      jdk: oraclejdk9
      addons:
        apt:
          packages:
            - oracle-java9-installer # Forces use of newer JDK 9 build
    - os: linux
      sudo: false
      jdk: openjdk8
    - os: linux
      sudo: false
      jdk: oraclejdk8

script:
  - sbt -no-colors clean +test
